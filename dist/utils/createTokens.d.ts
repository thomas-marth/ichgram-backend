import { Types } from "mongoose";
declare const createTokens: (id: Types.ObjectId) => {
    accessToken: string;
    refreshToken: string;
};
export default createTokens;
//# sourceMappingURL=createTokens.d.ts.map